# September 13, 2026 — Repeatable local backup and recovery

## Starting checkpoint
Verified GitHub branch `astra/implementation-20260905` exactly matched `6bd75b00e0771a6a9381943259c1fa9f303b154b` and the working tree was clean before this continuation. Freelancer OAuth application approval remains pending; this continuation performed no OAuth exchange or paid AI generation.

## R09 implementation and proof
- Added `scripts/backup_local.py`, which resolves the running Compose database and backend, retrieves the matching vault key privately, and invokes `pg_dump`/`pg_restore` from the exact source PostgreSQL image. The host has PostgreSQL 18 tools while the deployment runs PostgreSQL 16; using the image avoids repeating the prior tool-version mismatch.
- Encrypted archive includes the database and matching vault key. The recovery encryption key is generated once in owner-only local storage. Backup/key contents never appear in output, command arguments, Git, or audit documents.
- Restore targets are uniquely named disposable containers with fresh storage and loopback-only access. No application/worker process is attached. The drill confirms schema, user/job counts, decryption of restored credential records, and refusal to restore onto the resulting nonempty database. It removes the target container and its anonymous volume.
- Confirmed real local restoration: **1 user, 46 jobs, 1 credential decrypted**, schema `b39fa46fd835`; nonempty restore refused. Evidence: `09132026_implementation_evidence/local-recovery-final.log`. A private receipt alongside the archive records its SHA-256 and verified counts.
- Fixed the restore helper's prior database-before-key ordering. It now stages the matching key before invoking database restoration; an unwritable key destination cannot leave a restored database without that key file. Failed restore removes only the key file created by that invocation. Existing key files remain untouched.
- Three focused key-lifecycle tests passed: staging before restore, failure cleanup/existing-file protection, and refusal before database work when the key destination is unwritable. This change does not modify application runtime code, so the preceding 476-test application checkpoint is not relabeled as a new full-suite run.
- Installed local owner-only `gighound-backup.service` and `gighound-backup.timer`: daily 03:15 local time with up to 15 minutes jitter and missed-run persistence. A service invocation completed with `Result=success`, `ExecMainStatus=0`. No disposable recovery containers remained afterward.

## Owner action and remaining gates
Copy a verified encrypted archive and its receipt from `~/.local/state/gighound/backups/` off this machine. Store `~/.local/state/gighound/backup-recovery.key` separately in secure custody. No offsite upload was performed or claimed. Do not paste the recovery key into chat.

R09 remains open for offsite recovery, retention/capacity, unattended failure alerting and larger-than-256-MiB streaming backups. Forced process/host termination can require cleanup of uniquely labeled recovery containers. Live marketplace permissions, tenant AI budgets and all other open register items remain open. Requested the owner's primary service, target client and one real proof example to tailor business-first discovery and proposals without inventing experience.
