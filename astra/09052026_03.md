# GigHound audit execution evidence — September 5, 2026

This appendix records what was executed, what was only source-traced, and how to repeat the isolated counterexamples. No production platform actions or emails were performed. The main audit is [09052026_01.md](09052026_01.md); implementation plan is [09052026_02.md](09052026_02.md).

## Baseline and workspace safety

- Commit: `4f628609ac74ddd3b4bcaa0492435fa8d63b65e5`.
- Initial working tree: clean. Final tracked-code diff: empty; only audit Markdown files under `astra/` are added.
- Audit copy: `/tmp/gighound-astra-jRIMv4` (temporary paths are not permanent deliverables).
- Copied backend, worker, frontend and docs. Excluded `.env`, virtual environments, dependency directories and caches; reused existing interpreters and frontend dependencies. No dependency installation or application service deployment was necessary.
- The original backend test fixture flushes localhost Redis DB 15. Only in the copied fixture, changed its URL to `redis://127.0.0.1:1/15`. This avoids mutating any real Redis store, and intentionally leaves Redis-dependent tests skipped.
- All test DB URLs were explicit disposable SQLite paths. Mock transports/page doubles were used for external-action proofs. The two real browser checks used a blank in-memory page and a local Vite-only React harness, not a platform site or account.
- `.env` content was not read. A metadata-only check returned mode 0644; no permission fix was made.

## Executed results

| Check | Result | Meaning/limit |
|---|---|---|
| Backend pytest | 320 passed, 8 skipped, 225 warnings; 129.38 seconds | SQLite and Redis unavailable; not a Postgres/Redis production-equivalence result. |
| Worker pytest | 113 passed; 0.43 seconds | Existing mocked worker suite passes. |
| Frontend `npm run build` | TypeScript + Vite build passed | Built in temporary copy; runtime journeys still require testing. |
| Targeted audit proofs | 32 passed, 17 warnings | Assert observed behavior, including defect counterexamples; includes one negative control and one illustrative identity-collision example. Not 32 fixes. |
| Actual React hook in Chromium | First event handled: []; second event: [event-2] | Directly confirms the first-event loss. |
| Actual worker typing in Chromium | Plain text/newlines work; em dash and accented é fail | Tests actual Playwright behavior on a local textarea. |
| Python syntax | All 116 tracked Python files parsed successfully | No Python syntax error found; does not prove runtime correctness. |
| Alembic fresh SQLite upgrade | Reached head successfully | Fresh-schema migration smoke, not Postgres concurrency validation. |
| Alembic schema comparison | No new upgrade operations detected | SQLite metadata/schema comparison. |
| Historical duplicate fixture migration | Unique-index upgrade raises IntegrityError | Conditional deployment defect with two pre-existing live proposals. |
| Backend locked dependency audit | 50 packages, zero vulnerabilities reported | `pip-audit --no-deps --disable-pip` against lock, current service response. |
| Worker locked dependency audit | 15 packages, zero vulnerabilities reported | Same scope as backend audit. |
| Frontend dependency audit | Zero vulnerabilities reported, including dev packages | npm audit result; not a guarantee that dependencies contain no vulnerabilities. |

The frontend build reported a 331.31 kB JavaScript bundle (90.43 kB gzip) and 34.29 kB CSS (6.77 kB gzip). This is a measured baseline, not by itself a performance defect. Fonts are emitted in multiple subsets/formats; browser download behavior was not benchmarked.

## Audit-harness correction

The first backend run produced 25 failures: a deliberately chosen worker token differed from the suite’s hard-coded `test-worker-token`, and the temporary copy initially omitted `docs/api-contract.md`. Corrected those audit setup errors, copied docs, and reran the whole suite: 320 passed, 8 skipped. No application code changed to obtain that result. The original errors are excluded from the finding count.

Final proofing also corrected the accessibility claim after reading the shared Modal: role=dialog, aria-modal and Escape handling already exist. The retained finding concerns focus management and unassociated labels. Newline typing and zero-rate division-by-zero hypotheses were rejected with executable checks.

## Reproduction setup

Use a new disposable directory, not the live working tree. Copy the source without environment files or sessions. The following commands describe the actual final test setup; paths refer to this audit run. Generate a fresh local vault key for the proof process. The exported values below are synthetic test credentials, not deployment secrets.

```bash
# Existing regression suites (copied backend conftest points to port 1):
cd /tmp/gighound-astra-jRIMv4/backend
DATABASE_URL=sqlite:////tmp/gighound-astra-jRIMv4/test.db \
GIGHOUND_SECRET_KEY=test-secret-key GIGHOUND_WORKER_TOKEN=test-worker-token \
/home/dima/Work/Projects/gighound/backend/.venv/bin/python -m pytest -q -rs

cd /tmp/gighound-astra-jRIMv4
/home/dima/Work/Projects/gighound/worker/.venv/bin/python -m pytest -q worker/tests

cd /tmp/gighound-astra-jRIMv4/frontend
npm run build
```

For the proof source below, save it as `audit_proofs.py` in the disposable root. Set `PYTHONPATH` to both the copied backend and copied root; set `DATABASE_URL=sqlite://`, `REDIS_URL=redis://127.0.0.1:1/15`, synthetic signing/worker tokens and an ephemeral Fernet key. Run the backend interpreter with `python -m pytest audit_proofs.py -q`. The file is outside the original backend tests directory so its fixtures do not execute the original Redis FLUSHDB fixture.

## Targeted proof source

```python
import asyncio
from datetime import datetime, timedelta, timezone
from types import SimpleNamespace
from unittest.mock import AsyncMock, Mock
import pytest
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from app.database import Base
from app.models import User, Job, ProposalQueueItem, SearchProfile, SearchFilter, Template, StealthTask, PlatformAccount
from app.cache import cache

@pytest.fixture
def db(monkeypatch):
    monkeypatch.setattr(cache,'_r',None)
    monkeypatch.setattr(cache,'get_json',lambda *a,**k:None)
    monkeypatch.setattr(cache,'set_json',lambda *a,**k:None)
    from app import circuit_breaker
    circuit_breaker._local.clear(); circuit_breaker._local_trials.clear()
    engine=create_engine('sqlite://'); Base.metadata.create_all(engine)
    s=sessionmaker(bind=engine,expire_on_commit=False)()
    s.add(User(id=1,email='audit@example.test',password_hash='unused'))
    s.commit()
    yield s
    s.close(); engine.dispose()

def proposal(db, **kw):
    j=Job(user_id=1,platform=kw.pop('platform','upwork'),external_id='proof',title='React project',description='React development',quality_score=90)
    db.add(j); db.flush()
    p=ProposalQueueItem(user_id=1,job_id=j.id,platform=j.platform,proposal_text='old draft',humanized_text='old draft',**kw)
    db.add(p); db.commit(); return p

def test_edited_approval_keeps_old_browser_text(db):
    from app.routers.proposals import approve_proposal
    from app.schemas import ProposalReviewAction
    p=proposal(db)
    approve_proposal(p.id,ProposalReviewAction(reviewer='human',proposal_text='approved edit',save_as_template=False),db,db.get(User,1))
    assert p.proposal_text=='approved edit' and p.humanized_text=='old draft'

def test_tenant_can_open_global_circuit(db):
    from app.routers.gigs import set_circuit
    from app.circuit_breaker import check
    set_circuit('upwork',{'state':'open'},db.get(User,1))
    assert check('upwork',2)[0] is False

def test_all_profiles_opted_out_still_generate(db):
    from app.orchestrator import generation_gates_pass, build_pipeline_context
    p=proposal(db); j=db.get(Job,p.job_id); db.delete(p)
    db.add(SearchProfile(user_id=1,name='disabled automation',auto_queue_proposals=False)); db.commit()
    assert generation_gates_pass(db,j,build_pipeline_context(db,1)) is True

def test_unsupported_selected_platform_expands_discovery(db):
    from app.discovery import platforms_for_profile
    f=SearchFilter(user_id=1,name='Fiverr only',platforms=['fiverr']); db.add(f); db.flush()
    p=SearchProfile(user_id=1,name='Fiverr only',filter_id=f.id); db.add(p); db.commit()
    assert set(platforms_for_profile(db,p))=={'upwork','freelancer','linkedin'}

def test_outcome_repost_double_counts(db):
    from app.templates import record_outcome
    p=proposal(db,status='submitted'); t=Template(user_id=1,title='t',platform='upwork'); db.add(t); db.flush(); p.template_id=t.id; db.commit()
    record_outcome(db,p,'hired'); record_outcome(db,p,'hired'); record_outcome(db,p,'rejected')
    assert (t.wins,t.losses)==(2,1) and p.outcome=='rejected'

def test_reaper_requeues_irreversible_submission(db,monkeypatch):
    from app import tasks
    p=proposal(db,status='queued_for_browser')
    t=StealthTask(user_id=1,platform='upwork',task_type='submit_upwork_proposal',payload={'proposal_queue_item_id':p.id},status='claimed',claimed_by='dead',claimed_at=datetime.now(timezone.utc)-timedelta(hours=1))
    db.add(t);db.commit();monkeypatch.setattr(tasks,'SessionLocal',lambda:db)
    result=tasks.stealth_reaper_tick_core()
    assert t.id in result['reclaimed'] and t.status=='pending'

def test_reaper_final_failure_strands_proposal(db,monkeypatch):
    from app import tasks
    p=proposal(db,status='queued_for_browser')
    t=StealthTask(user_id=1,platform='upwork',task_type='submit_upwork_proposal',payload={'proposal_queue_item_id':p.id},status='claimed',claimed_by='dead',claimed_at=datetime.now(timezone.utc)-timedelta(hours=1),reclaim_count=3)
    db.add(t);db.commit();monkeypatch.setattr(tasks,'SessionLocal',lambda:db)
    tasks.stealth_reaper_tick_core()
    assert t.status=='failed' and p.status=='queued_for_browser'

def test_unverified_cannot_be_marked_submitted(db):
    from app.routers.proposals import mark_submitted
    from fastapi import HTTPException
    p=proposal(db,status='submitted_unverified')
    with pytest.raises(HTTPException) as e: mark_submitted(p.id,None,db,db.get(User,1))
    assert e.value.status_code==409

def test_enrolled_token_missing_required_expiry(db):
    from app.routers.credentials import _validate_secrets
    from app.adapters.vault import CredentialVault
    from app.adapters.freelancer import FreelancerAdapter
    secrets=_validate_secrets('freelancer',{'access_token':'test'})
    CredentialVault(db,1).store('freelancer','default',secrets)
    a=FreelancerAdapter(db,1)
    try:
      with pytest.raises(KeyError,match='expires_at'): asyncio.run(a._access_token())
    finally: asyncio.run(a.close())

def test_password_validation_counts_characters_not_bytes(db):
    from app.schemas import RegisterIn
    from app.auth import hash_password
    p=RegisterIn(email='a@b.test',password='é'*40)
    with pytest.raises(ValueError): hash_password(p.password)

def test_invalid_version_index_raises_typeerror(db):
    from app.routers.proposals import revert_version
    p=proposal(db,versions=[{'text':'old'}])
    with pytest.raises(TypeError): revert_version(p.id,{'version_index':'0'},db,db.get(User,1))

def test_blank_worker_verdict_means_submitted(db):
    from app.routers.gigs import _apply_submission_outcome
    p=proposal(db,status='queued_for_browser',platform='fiverr')
    t=StealthTask(user_id=1,platform='fiverr',task_type='submit_fiverr_offer',payload={'proposal_queue_item_id':p.id},result={})
    _apply_submission_outcome(db,t,True)
    assert p.status=='submitted'

def test_follow_up_dispatches_bid(db,monkeypatch):
    from app.routers.proposals import submit_proposal
    from app.adapters.freelancer import FreelancerAdapter
    p=proposal(db,platform='freelancer',status='approved',request_type='follow_up',reviewed_by='human',submission_result={'bidder_id':12})
    j=db.get(Job,p.job_id);j.external_id='123';db.commit()
    bid=AsyncMock(return_value={'id':5});monkeypatch.setattr(FreelancerAdapter,'place_bid',bid)
    asyncio.run(submit_proposal(p.id,db,db.get(User,1)))
    assert bid.call_args.kwargs['amount']==0 and bid.call_args.kwargs['proposal']=='old draft'

def test_claim_ignores_disabled_account(db):
    from app.routers.gigs import claim_stealth_task
    from app.schemas import StealthTaskClaimIn
    db.add(PlatformAccount(user_id=1,platform='upwork',label='disabled',enabled=False,mode='disabled'))
    t=StealthTask(user_id=1,platform='upwork',task_type='submit_upwork_proposal',payload={});db.add(t);db.commit()
    assert claim_stealth_task(t.id,StealthTaskClaimIn(worker_id='proof'),db,'worker')['status']=='claimed'

def test_empty_dictionary_inputs_raise_500_causes(db):
    from app.routers.gigs import ingest_competitor_snapshot
    with pytest.raises(KeyError): ingest_competitor_snapshot({},db,db.get(User,1))

def test_expired_job_passes_filter(db):
    from app.filtering import job_matches_filter
    p=proposal(db);j=db.get(Job,p.job_id);j.apply_deadline=datetime.now(timezone.utc)-timedelta(days=1)
    f=SearchFilter(user_id=1,name='due soon',apply_deadline_within_hours=24,quality_threshold=40);db.add(f);db.commit()
    assert job_matches_filter(j,f)[0] is True

def test_legacy_bid_repeat_and_wrong_platform(db,monkeypatch):
    from app.routers.adapters import freelancer_bid, QueueItemAction
    from app.adapters.freelancer import FreelancerAdapter
    p=proposal(db,status='approved',reviewed_by='human',submission_result={'bidder_id':1})
    db.get(Job,p.job_id).external_id='123';db.commit()
    bid=AsyncMock(return_value={'id':5});monkeypatch.setattr(FreelancerAdapter,'place_bid',bid)
    monkeypatch.setattr(FreelancerAdapter,'bids_remaining',lambda self:50)
    for _ in range(2): asyncio.run(freelancer_bid(QueueItemAction(proposal_queue_item_id=p.id),db,db.get(User,1)))
    assert bid.call_count==2 and p.status=='approved' and p.platform=='upwork'

def test_mutating_post_is_retried(db,monkeypatch):
    import httpx
    from app.adapters import ratelimit
    calls=[]
    def handler(req):
      calls.append(req);return httpx.Response(503 if len(calls)==1 else 200,json={'id':1})
    monkeypatch.setattr(ratelimit.asyncio,'sleep',AsyncMock())
    async def run():
      async with httpx.AsyncClient(transport=httpx.MockTransport(handler)) as c:
        await ratelimit.request_with_retry(c,'POST','https://example.test/bids',data={'amount':10})
    asyncio.run(run()); assert len(calls)==2

def test_profile_lock_starves_second_profile(db,monkeypatch):
    from app import discovery
    keys=set()
    def acquire(key,*a,**k):
      if key in keys:return False
      keys.add(key);return True
    monkeypatch.setattr(discovery.cache,'_r',SimpleNamespace(set=acquire))
    search=AsyncMock(return_value=[]);monkeypatch.setattr(discovery,'_search_platform',search)
    monkeypatch.setattr(discovery,'platforms_for_profile',lambda *a:['freelancer'])
    user=db.get(User,1)
    for name in ['React','Python']:
      p=SearchProfile(user_id=1,name=name);db.add(p);db.commit();asyncio.run(discovery.run_profile_discovery(db,user,p))
    assert search.call_count==1

def test_foreign_keyword_group_can_be_referenced(db,monkeypatch):
    from app.models import KeywordGroup
    from app.schemas import SearchFilterIn
    from app.routers.filters import create_filter
    monkeypatch.setattr(cache,'invalidate_prefix',lambda *a:None)
    db.add(User(id=2,email='other@example.test',password_hash='unused'));db.flush()
    g=KeywordGroup(user_id=2,name='foreign');db.add(g);db.commit()
    f=create_filter(SearchFilterIn(name='foreign ref',keyword_group_id=g.id),db,db.get(User,1))
    assert f.user_id==1 and f.keyword_group_id==g.id

def test_same_second_other_token_survives_password_revocation(db,monkeypatch):
    from app import auth
    monkeypatch.setattr(cache,'get_json',lambda key:1000 if 'notbefore' in key else None)
    assert auth._is_token_revoked({'sub':'1','iat':1000,'jti':'another-token'}) is False

def test_websocket_holds_database_transaction(db,monkeypatch):
    from app.routers import alerts as router
    from fastapi import WebSocketDisconnect
    monkeypatch.setattr(router,'DEV_NOAUTH',False)
    monkeypatch.setattr(router,'_consume_ws_ticket',lambda t:None)
    monkeypatch.setattr(router,'get_user_from_token',lambda s,t:s.get(User,1))
    monkeypatch.setattr(router.alerts,'connect',AsyncMock())
    monkeypatch.setattr(router.alerts,'disconnect',Mock())
    active=[]
    class WS:
      async def receive_text(self):
        active.append(db.in_transaction());raise WebSocketDisconnect()
    db.commit();asyncio.run(router.alerts_ws(WS(),'test',None,db));assert active==[True]

def test_buyer_offer_contains_unsupported_experience_claim(db):
    from app.fiverr_monitor import generate_custom_offer
    assert 'done similar work this month' in generate_custom_offer({'title':'Anything'})

def test_hourly_rate_currency_ignored(db):
    from app.models import RateCardEntry
    from app.proposal_gen import calculate_bid
    p=proposal(db); j=db.get(Job,p.job_id);j.job_type='hourly'
    r=RateCardEntry(user_id=1,skill_category='React',hourly_rate=100,currency='EUR');db.add(r);db.commit()
    amount,_,reason=calculate_bid(db,j,{},entries=[r]); assert amount==100 and '$100' in reason

def test_zero_rate_can_crash_score(db):
    from app.scoring import compute_quality_score
    from app.schemas import JobIngest
    j=JobIngest(platform='upwork',external_id='x',title='x',job_type='hourly',budget_min=100)
    # zero falls back to default, so it is NOT a division-by-zero finding
    assert isinstance(compute_quality_score(j,market_rate=0)['quality_score'],float)

def test_status_match_confuses_identifier_prefix():
    from worker.handlers.proposal_status import _matches,canonical_status
    assert _matches({'job_external_id':'123'},None,'','https://example.test/job/1234')
    assert canonical_status('contract declined')=='hired'

def test_task_result_can_change_unlisted_other_platform_proposal(db):
    from app.proposal_status_sync import apply_proposal_status_results
    p=proposal(db,platform='freelancer',status='pending_review')
    t=StealthTask(user_id=1,platform='upwork',task_type='scrape_proposal_status',payload={'items':[]})
    result=asyncio.run(apply_proposal_status_results(db,t,[{'proposal_queue_item_id':p.id,'platform_status':'hired'}]))
    assert result['outcomes']==1 and p.outcome=='hired'

def test_client_identity_merges_distinct_anonymous_clients(db):
    from app.client_intel import client_key_for
    assert client_key_for({'country':'US','total_spent':0},'upwork')==client_key_for({'country':'US','total_spent':0},'upwork')

def test_successful_manual_assist_has_no_success_marker_check():
    from worker.handlers import manual_assist as m
    from worker.platforms import platform_config
    from unittest.mock import patch
    page=Mock();ctx=SimpleNamespace(config=SimpleNamespace(allow_submit_for=lambda p:True),browser=Mock())
    task=SimpleNamespace(payload={'url':'https://example.test','proposal_text':'x'},platform='fiverr',user_id=1,id=1,task_type='submit_fiverr_offer')
    with patch.object(m,'fetch_page',return_value=page),patch.object(m,'type_with_plan'),patch.object(m,'human_delay'),patch.object(m,'raise_if_challenge'):
      result=m.handle_submit_fiverr_offer(task,ctx)
    assert result['submitted'] is True

def test_validated_fiverr_template_breaks_worker_description(db,monkeypatch):
    from app.gig_templates import validate_fiverr_template
    from worker.handlers import gig_draft as g
    from worker import browser as b
    template={'title':'I will build a great website','category':'Programming & Tech','subcategory':'Website Development','description':{'hook':'hello','what_you_get':'website','why_me':'experience','cta':'message'},'pricing':{'basic':{'price':100,'delivery_days':3}}}
    assert validate_fiverr_template(template)==[]
    page=Mock();ctx=SimpleNamespace(browser=Mock());task=SimpleNamespace(payload={'template':template},platform='fiverr',user_id=1,id=1)
    monkeypatch.setattr(g,'fetch_page',lambda *a:page);monkeypatch.setattr(g,'human_delay',lambda *a:None);monkeypatch.setattr(b,'_sleep',lambda *a:None)
    with pytest.raises(AttributeError,match='split'):g.handle_create_gig_draft(task,ctx)

def test_gig_metrics_replay_creates_duplicate_week(db):
    from app.models import Gig,GigMetric
    from app.gig_analytics import record_metrics
    g=Gig(user_id=1,platform='fiverr',title='gig');db.add(g);db.commit()
    for _ in range(2):record_metrics(db,g,100,10,1,20,'2026-W36')
    assert db.query(GigMetric).filter_by(gig_id=g.id,week='2026-W36').count()==2

def test_job_requirements_are_misclassified_as_user_skills(db):
    from app.proposal_gen import skill_portfolio_match
    p=proposal(db);j=db.get(Job,p.job_id);j.skills=['React'];db.commit()
    result=skill_portfolio_match(db,j,{'required_skills':['React']},items=[])
    assert result['strengths']==['react'] and result['gaps']==[] and result['items']==[]

```

## Actual browser output

```text
Actual React hook after first event: []
Actual React hook after second event: ["event-2"]
'Hello\nWorld' => 'Hello\nWorld'
'Hello — world' => Keyboard.press: Unknown key: "—"
'café' => Keyboard.press: Unknown key: "é"
'plain text' => 'plain text'
```

## React harness source

The disposable `audit.html` loaded this module, served by local Vite on 127.0.0.1:4179. Chromium clicked Send twice with an event-loop yield between clicks and read `#seen`.

```tsx
import React, {useState} from 'react';
import {createRoot} from 'react-dom/client';
import {useNewAlertMessages} from './src/hooks/useAlertsSocket';
function Audit(){
 const [messages,setMessages]=useState<any[]>([]);
 const [seen,setSeen]=useState<string[]>([]);
 useNewAlertMessages(messages,m=>setSeen(p=>[...p,m.type]));
 return <><button id="send" onClick={()=>setMessages(p=>[{type:`event-${p.length+1}`,receivedAt:Date.now()},...p])}>send</button><pre id="seen">{JSON.stringify(seen)}</pre></>;
}
createRoot(document.getElementById('root')!).render(<Audit/>);
```

## Migration evidence

```text
INFO  [alembic.runtime.migration] Context impl SQLiteImpl.
INFO  [alembic.runtime.migration] Will assume non-transactional DDL.
INFO  [alembic.runtime.migration] Running upgrade  -> 2a47c215a114, initial schema with users and tenancy
INFO  [alembic.runtime.migration] Running upgrade 2a47c215a114 -> 6d06a7fdf7dd, add platform account settings
INFO  [alembic.runtime.migration] Running upgrade 6d06a7fdf7dd -> 0adef05255f0, add proposal reply tracking (client_replied_at, save_as_template)
INFO  [alembic.runtime.migration] Running upgrade 0adef05255f0 -> 7c1e9a2b4d38, add bid_advice to proposal_queue (Phase 3.4)
INFO  [alembic.runtime.migration] Running upgrade 7c1e9a2b4d38 -> b3f1c9d42e10, add stealth task claim columns (claimed_by, claimed_at)
INFO  [alembic.runtime.migration] Running upgrade b3f1c9d42e10 -> c4e2a81f05b7, add client_key to jobs (indexed client identity for keyed history lookups)
INFO  [alembic.runtime.migration] Running upgrade c4e2a81f05b7 -> d3f8a2c91e07, add stealth task reclaim_count (reaper resets dead-worker claims)
INFO  [alembic.runtime.migration] Running upgrade d3f8a2c91e07 -> e4a91b6c2d08, partial unique index: one live generated proposal per job
INFO  [alembic.runtime.migration] Context impl SQLiteImpl.
INFO  [alembic.runtime.migration] Will assume non-transactional DDL.
INFO  [alembic.runtime.plugins] setting up autogenerate plugin alembic.autogenerate.schemas
INFO  [alembic.runtime.plugins] setting up autogenerate plugin alembic.autogenerate.tables
INFO  [alembic.runtime.plugins] setting up autogenerate plugin alembic.autogenerate.types
INFO  [alembic.runtime.plugins] setting up autogenerate plugin alembic.autogenerate.constraints
INFO  [alembic.runtime.plugins] setting up autogenerate plugin alembic.autogenerate.defaults
INFO  [alembic.runtime.plugins] setting up autogenerate plugin alembic.autogenerate.comments
INFO  [alembic.runtime.plugins] setting up autogenerate plugin alembic.autogenerate.checkconstraint_byname
No new upgrade operations detected.
INFO  [alembic.runtime.migration] Context impl SQLiteImpl.
INFO  [alembic.runtime.migration] Will assume non-transactional DDL.
INFO  [alembic.runtime.migration] Running upgrade  -> 2a47c215a114, initial schema with users and tenancy
INFO  [alembic.runtime.migration] Running upgrade 2a47c215a114 -> 6d06a7fdf7dd, add platform account settings
INFO  [alembic.runtime.migration] Running upgrade 6d06a7fdf7dd -> 0adef05255f0, add proposal reply tracking (client_replied_at, save_as_template)
INFO  [alembic.runtime.migration] Running upgrade 0adef05255f0 -> 7c1e9a2b4d38, add bid_advice to proposal_queue (Phase 3.4)
INFO  [alembic.runtime.migration] Running upgrade 7c1e9a2b4d38 -> b3f1c9d42e10, add stealth task claim columns (claimed_by, claimed_at)
INFO  [alembic.runtime.migration] Running upgrade b3f1c9d42e10 -> c4e2a81f05b7, add client_key to jobs (indexed client identity for keyed history lookups)
INFO  [alembic.runtime.migration] Running upgrade c4e2a81f05b7 -> d3f8a2c91e07, add stealth task reclaim_count (reaper resets dead-worker claims)
INFO  [alembic.runtime.migration] Context impl SQLiteImpl.
INFO  [alembic.runtime.migration] Will assume non-transactional DDL.
INFO  [alembic.runtime.migration] Running upgrade d3f8a2c91e07 -> e4a91b6c2d08, partial unique index: one live generated proposal per job
CONFIRMED upgrade with existing duplicate live rows fails: IntegrityError (sqlite3.IntegrityError) UNIQUE constraint failed: proposal_queue.job_id
```

The historical fixture upgraded to `d3f8a2c91e07`, inserted a synthetic user/job and two `pending_review`, `request_type=job` proposals, then attempted `upgrade head`. No existing deployment data was inspected.

## Redis-dependent checks not exercised

```text
SKIPPED [1] tests/test_auth.py:235: Redis unavailable
SKIPPED [1] tests/test_auth.py:244: Redis unavailable
SKIPPED [1] tests/test_auth.py:270: Redis unavailable
SKIPPED [1] tests/test_security_p1.py:229: Redis unavailable
SKIPPED [1] tests/test_security_p1.py:238: Redis unavailable
SKIPPED [1] tests/test_security_p1.py:250: Redis unavailable
SKIPPED [1] tests/test_security_p1.py:257: Redis unavailable
SKIPPED [1] tests/test_security_p1.py:339: Redis unavailable
```

These include session revocation and rate-limit behavior requiring a live Redis instance. Source review and isolated mocks do not substitute for those integration checks. A future implementation must run them against a disposable Redis/Valkey instance.

## Source inventory and review coverage

The inventory below identifies tracked files included in the audit baseline. Deep source tracing focused on backend routes/domain state, adapters, workers, schema/migrations, frontend hooks/API and relevant view handlers. All frontend views were included in build and targeted source review, but this is not a claim of exhaustive visual inspection of every screen. Historical audit documents are context, not current proof.

```text
.dockerignore
.env.example
.github/dependabot.yml
.github/workflows/ci.yml
.gitignore
README.md
backend/Dockerfile
backend/alembic.ini
backend/alembic/env.py
backend/alembic/script.py.mako
backend/alembic/versions/.gitkeep
backend/alembic/versions/0adef05255f0_add_proposal_reply_tracking.py
backend/alembic/versions/2a47c215a114_initial_schema_with_users_and_tenancy.py
backend/alembic/versions/6d06a7fdf7dd_add_platform_account_settings.py
backend/alembic/versions/7c1e9a2b4d38_add_bid_advice_to_proposal_queue.py
backend/alembic/versions/b3f1c9d42e10_add_stealth_task_claim_columns.py
backend/alembic/versions/c4e2a81f05b7_add_client_key_to_jobs.py
backend/alembic/versions/d3f8a2c91e07_add_stealth_task_reclaim_count.py
backend/alembic/versions/e4a91b6c2d08_proposal_queue_live_job_unique.py
backend/app/__init__.py
backend/app/adapters/__init__.py
backend/app/adapters/base.py
backend/app/adapters/freelancer.py
backend/app/adapters/linkedin.py
backend/app/adapters/ratelimit.py
backend/app/adapters/schema.py
backend/app/adapters/upwork_agency.py
backend/app/adapters/vault.py
backend/app/antidetect.py
backend/app/auth.py
backend/app/boolquery.py
backend/app/cache.py
backend/app/circuit_breaker.py
backend/app/client_intel.py
backend/app/config.py
backend/app/database.py
backend/app/digest.py
backend/app/discovery.py
backend/app/filtering.py
backend/app/fiverr_monitor.py
backend/app/follow_up.py
backend/app/gig_analytics.py
backend/app/gig_templates.py
backend/app/ingest.py
backend/app/llm.py
backend/app/main.py
backend/app/models.py
backend/app/orchestrator.py
backend/app/outcome_sync.py
backend/app/platforms.py
backend/app/proposal_gen.py
backend/app/proposal_status_sync.py
backend/app/rate_learning.py
backend/app/ratelimit.py
backend/app/routers/__init__.py
backend/app/routers/adapters.py
backend/app/routers/alerts.py
backend/app/routers/analytics.py
backend/app/routers/auth.py
backend/app/routers/credentials.py
backend/app/routers/filters.py
backend/app/routers/gigs.py
backend/app/routers/jobs.py
backend/app/routers/keywords.py
backend/app/routers/orchestration.py
backend/app/routers/profiles.py
backend/app/routers/proposals.py
backend/app/schemas.py
backend/app/scoring.py
backend/app/skills_taxonomy.py
backend/app/stealth.py
backend/app/tasks.py
backend/app/templates.py
backend/app/textgen.py
backend/app/ws_manager.py
backend/requirements-dev.txt
backend/requirements.lock
backend/requirements.txt
backend/scripts/__init__.py
backend/scripts/seed_defaults.py
backend/tests/conftest.py
backend/tests/test_account_lifecycle.py
backend/tests/test_adapters.py
backend/tests/test_auth.py
backend/tests/test_cluster_f.py
backend/tests/test_cluster_u.py
backend/tests/test_credentials.py
backend/tests/test_docs_contract.py
backend/tests/test_generation_gigs.py
backend/tests/test_orchestration.py
backend/tests/test_phase0.py
backend/tests/test_phase1.py
backend/tests/test_phase2.py
backend/tests/test_phase3.py
backend/tests/test_phase5.py
backend/tests/test_ratelimit.py
backend/tests/test_reaudit1.py
backend/tests/test_retention.py
backend/tests/test_security_p1.py
backend/tests/test_stealth_reaper.py
backend/tests/test_stealth_worker.py
backend/tests/test_textgen.py
backend/tests/test_wave2.py
docker-compose.worker.yml
docker-compose.yml
docs/api-contract.md
docs/environment.md
docs/platform-intelligence-report.md
evolution/README.md
evolution/architecture-decisions.md
evolution/cross-examination.md
evolution/final-scorecard.md
evolution/implementation-log.md
evolution/pass1-systems-audit.md
evolution/pass2-security-compliance-audit.md
evolution/pass3-product-strategy-audit.md
evolution/plan-of-attack.md
evolution/review-2026-08-31/cross-verification.md
evolution/review-2026-08-31/implementation-plan.md
evolution/review-2026-08-31/pass-1-supply-chain.md
evolution/review-2026-08-31/pass-2a-backend-coupling.md
evolution/review-2026-08-31/pass-2b-frontend-infra-coupling.md
evolution/review-2026-08-31/pass-3a-backend-security.md
evolution/review-2026-08-31/pass-3b-pipeline-correctness.md
evolution/review-2026-08-31/pass-3c-worker.md
evolution/review-2026-08-31/pass-3d-frontend-infra.md
frontend/Dockerfile
frontend/index.html
frontend/nginx.conf
frontend/package-lock.json
frontend/package.json
frontend/public/manifest.webmanifest
frontend/src/App.tsx
frontend/src/api/client.ts
frontend/src/components/AccountModals.tsx
frontend/src/components/OnboardingChecklist.tsx
frontend/src/components/common.tsx
frontend/src/hooks/useAlertsSocket.ts
frontend/src/hooks/useDrafts.ts
frontend/src/main.tsx
frontend/src/styles.css
frontend/src/types.ts
frontend/src/views/Accounts.tsx
frontend/src/views/AlertsPanel.tsx
frontend/src/views/Analytics.tsx
frontend/src/views/BuyerRequestInbox.tsx
frontend/src/views/GigManager.tsx
frontend/src/views/JobFeed.tsx
frontend/src/views/KeywordIntelligence.tsx
frontend/src/views/Login.tsx
frontend/src/views/ProfileManager.tsx
frontend/src/views/ProposalQueue.tsx
frontend/src/views/ScoringConfig.tsx
frontend/src/views/SearchFineTuning.tsx
frontend/src/views/SearchProfiles.tsx
frontend/src/vite-env.d.ts
frontend/tsconfig.json
frontend/vite.config.ts
scripts/bootstrap.sh
worker/.dockerignore
worker/Dockerfile
worker/README.md
worker/__init__.py
worker/__main__.py
worker/browser.py
worker/client.py
worker/config.py
worker/handlers/__init__.py
worker/handlers/base.py
worker/handlers/buyer_requests.py
worker/handlers/gig_draft.py
worker/handlers/manual_assist.py
worker/handlers/proposal_status.py
worker/handlers/scrape.py
worker/handlers/upwork_proposal.py
worker/login.py
worker/platforms.py
worker/requirements-dev.txt
worker/requirements.lock
worker/requirements.txt
worker/runner.py
worker/tests/conftest.py
worker/tests/test_browser_helpers.py
worker/tests/test_buyer_requests.py
worker/tests/test_client.py
worker/tests/test_config.py
worker/tests/test_fingerprint.py
worker/tests/test_gig_draft.py
worker/tests/test_proposal_status.py
worker/tests/test_runner.py
worker/tests/test_stealth_phase2.py
```

## Finding proof index

| Finding | Evidence method |
|---|---|
| A001 | test_edited_approval_keeps_old_browser_text |
| A002 | test_legacy_bid_repeat_and_wrong_platform |
| A003 | test_mutating_post_is_retried |
| A004 | test_reaper_requeues_irreversible_submission; test_reaper_final_failure_strands_proposal |
| A005 | Source interleaving; not reproduced under real concurrent Postgres workers |
| A006 | Source-level concurrency proof; production frequency unknown |
| A007 | test_blank_worker_verdict_means_submitted |
| A008 | Real Chromium browser-proofs.log; not a mock |
| A009 | test_validated_fiverr_template_breaks_worker_description |
| A010 | test_successful_manual_assist_has_no_success_marker_check; runner cleanup trace |
| A011 | test_unverified_cannot_be_marked_submitted; UI/API trace |
| A012 | test_follow_up_dispatches_bid |
| A013 | test_tenant_can_open_global_circuit |
| A014 | test_claim_ignores_disabled_account; source trace |
| A015 | test_enrolled_token_missing_required_expiry |
| A016 | Source producer/consumer identity trace |
| A017 | Source/API/UI contract trace |
| A018 | Source proof of missing binding; exploit outcome not live-verified |
| A019 | Reachability source trace; no internal target contacted |
| A020 | Source trust-boundary proof |
| A021 | test_same_second_other_token_survives_password_revocation; failure-path trace |
| A022 | test_websocket_holds_database_transaction |
| A023 | Direct recipient construction proof; no email sent |
| A024 | Model, write-site and deletion trace |
| A025 | test_password_validation_counts_characters_not_bytes |
| A026 | test_invalid_version_index_raises_typeerror; test_empty_dictionary_inputs_raise_500_causes; schema inspection |
| A027 | test_foreign_keyword_group_can_be_referenced |
| A028 | test_profile_lock_starves_second_profile |
| A029 | test_all_profiles_opted_out_still_generate |
| A030 | test_unsupported_selected_platform_expands_discovery |
| A031 | Cross-stage source proof; intended product semantics require a documented decision |
| A032 | test_expired_job_passes_filter; ingest and dispatch trace |
| A033 | Producer/recovery query source proof |
| A034 | Source interleaving and gate comparison; concurrency not live-load tested |
| A035 | test_outcome_repost_double_counts |
| A036 | Source accounting trace |
| A037 | test_hourly_rate_currency_ignored; schema and normalization trace |
| A038 | Source collision construction; synthetic test is illustrative, not evidence of a real client merge |
| A039 | Analytics predicates and follow-up query proof |
| A040 | test_status_match_confuses_identifier_prefix |
| A041 | test_task_result_can_change_unlisted_other_platform_proposal |
| A042 | Deterministic source scenario with 50 older read tasks |
| A043 | Source lifetime, counter and dispatch trace |
| A044 | Call-site search and timing constants |
| A045 | test_gig_metrics_replay_creates_duplicate_week; partial extraction trace |
| A046 | Real React/Chromium browser-proofs.log |
| A047 | Source async interleaving; full UI race not browser-reproduced |
| A048 | Component and discovery-return trace |
| A049 | Static JSX evidence; no formal WCAG certification claimed |
| A050 | Deployment/source inventory |
| A051 | Source query/algorithm bounds |
| A052 | migration-duplicates.log |
| A053 | 320 backend passes/8 Redis skips; 113 worker passes; passing build plus independent counterexamples |
| A054 | stat -c mode check only |
| A055 | test_buyer_offer_contains_unsupported_experience_claim; generator literals |
| A056 | Official sources checked September 5, 2026; see source note below |
| A057 | File writes and absence of lifecycle consumer across worker source |
| A058 | Compose/repository inventory; external infrastructure not inspected |
| A059 | Route/UI inventory |
| A060 | Source structure, warnings and manifests |
| A061 | test_job_requirements_are_misclassified_as_user_skills |
| A062 | Source/UI-client search; no matching settings.username or agency/members consumer found |

## Final verification checklist

- All 62 finding IDs map to an implementation phase; the feature list has exactly 10 entries.
- Source references resolve to existing files and line numbers checked against the audited tree.
- Every finding states its mechanism and distinguishes observed behavior, source-proven conditions, and untested production consequences.
- Existing repairs are credited; superseded historical claims are not copied as new defects.
- Local source remained unchanged. Temporary test fixtures, generated builds and browser harnesses stayed outside the project; only these audit Markdown documents are deliverables.
- No audit result claims exhaustive safety, live-platform compatibility, legal approval, production performance or guaranteed exponential commercial returns.
