# Dev/test-only dependencies — production installs (Dockerfile, deployment)
# use requirements.txt alone. Install both for local development:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt
pytest==8.*
